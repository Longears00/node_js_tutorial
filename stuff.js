
var counter = function (arr) {
  return 'this is conter function';
};

var adder = function (a, b) {
  return `the sum off is ${a + b}`;
};

var pi = 3.142;

module.exports = {
    counter: counter,
    adder: adder,
    pi: pi,
  };
